# anyflow-embed 構築引き継ぎドキュメント

別の Claude アカウント／セッションでこの構築の続きを行うためのドキュメント。
これを読めば、現状の実装内容・設計判断・作業の続け方が再現できる。

最終更新: 2026-08-13（KV以降の全セクション実装済み。§7 参照）

---

## 1. プロジェクト概要

- **内容**: Anyflow Embed のトップページ（キービジュアル）。Figma カンプの忠実再現＋アニメーション実装
- **本番 URL**: https://anyflow-embed.vercel.app
- **場所**: 親リポ `hideyuki-yamanaka.github.io` の `anyflow-embed/` サブディレクトリ（submodule ではない）
- **構成**: `index.html` 単一ファイル（フレームワーク・ビルドなし）+ `assets/`
- **デプロイ**: 手動。`anyflow-embed/` ディレクトリで `npx vercel --prod --yes`
  （Vercel プロジェクト名: `anyflow-embed`。git push では自動デプロイされない）
- **ブランチ運用**: main 一本。feature branch 禁止（親リポの CLAUDE.md 参照）
- **ローカル確認**: `.claude/launch.json` の `anyflow-embed`（port 8773 の python http.server）

## 2. Figma ソース

ファイルキー: `jSLFEubHMoy3Hxgcw1AZuR`（works）

| ノード | 内容 |
|---|---|
| `14286:15414` | TOP カンプ本体（レイアウト・軌道・ドット座標の出典） |
| `14329:12869` | 惑星デザイン違い 3 案のセクション（B/C/D の出典） |
| `14329:13846` | 惑星 E（ハーフトーン）＋軌道形状違いのセクション |
| `14329:12780` / `12868` / `12692` | 惑星 B / C / D の Vector ノード |
| `14329:13580` | 惑星 E の Vector ノード |

- 惑星テクスチャは **defaultScale 4 で PNG 書き出し**したもの（`assets/planet-b〜e.png`、約950px）
- Figma MCP の asset URL は約7日で失効するので、再取得時は `download_assets` を使う

## 3. レイアウトの数値（カンプ準拠）

- ステージ: **1440×921 固定**を `transform: scale()` で画面幅にフィット（`fit()`）
- 見出し: left 109px / top 210px、Noto Sans JP Thin、50px / 80px / 80px
- 軌道グループ: left 496px / top 179px、915.483×630
- 楕円（`ELLIPSES`）: outer = cx457.741, cy306.543, rx435.517, ry146.026 ／ inner = cx475.283, cy350.505, rx397.09, ry133.142、基本回転 `ROTDEG = -23.3266°`
- 軌道線: stroke 3px の線形グラデ（outer: ピンク→白→水色 / inner: 水色→白→ピンク、`userSpaceOnUse` で cy±ry）
- ドット6個: r=5.606。所属楕円と初期角度は `DOTS` 配列（カンプ座標から楕円式で逆算済み）
- 惑星: 230×230、ステージ座標 left 817 / top 306（canvas は余白込み 260×260）
- ロゴ: `logos-strip.png` を2枚並べた CSS マーキー（top 796）

## 4. アーキテクチャ

### 4.1 奥行き（前後）の仕組み

軌道は **奥レイヤー SVG → 惑星 canvas → 手前レイヤー SVG** の3層。

- 奥 SVG: 楕円の全周＋「奥側を通行中のドット」
- 手前 SVG: 楕円の手前半分（clipPath で下半分を切り出し）＋「手前側のドット」
- ドットはパラメトリック角度の `sin(t) > 0` が手前。`renderFrame()` で毎フレーム表示レイヤーを切替
- これで奥を通る線・ドットは惑星の裏に隠れる（カンプの重なりと一致）

軌道のジオメトリはパネル設定を反映して毎フレーム `orbitGeom()` → `updateOrbitSvg()` で
SVG属性（cx/cy/rx/ry/transform、クリップ矩形、グラデ座標）を再計算する。
ドット位置も同じジオメトリから計算するので、軌道をいじってもズレない。

### 4.2 惑星（WebGL）

- 素の WebGL（ライブラリなし）。フルスクリーントライアングル＋フラグメントシェーダー
- **惑星ごとに別プログラムをコンパイル**（`DESIGNS` の各エントリ）
- B/C/D: カンプ画像をテクスチャとして球面に貼る方式
  - `uv = (0.5 + d.x*0.47, 0.5 - d.y*0.47)`（d = 回転後の法線）
  - 無回転時はカンプがピクセル一致で表示される。裏側はカンプに無いので鏡映で生成
  - デザイン別の `finish`（GLSL片）で粒・ハイライト・リムを仕上げ（B: 乗算粗粒 / C: 境界中粒 / D: 高密度微粒）
- E（ハーフトーンブルー）: **完全生成**（`fragment` に独自シェーダー全体を持つ。テクスチャ不使用＝継ぎ目なし）
  - 明るさの場 `lum` → 6段階量子化 ＋ ベイヤーディザ（2px セル）でピクセル調
  - カラーランプ: マゼンタ⇄紫⇄深青⇄青⇄シアン⇄白（`ramp()`、負の値で紫帯）
  - ディザ演出（`uDither`）: ①固定 ②うつろい ③流れ ④さざ波 — **色の場を波で動かして**境目のピクセルがパラパラめくれる方式（しきい値だけ動かす方式はNGだった経緯あり）
- 回転: 軌道の傾きとはずらした2軸合成（`ax1`, `ax2`、`uAngle`/`uAngle2`）。上下の位置揺れはなし
- ライト（`uLight`）: ①なし ②右上光源。②は右上からの平行光＋影は**青みに沈める**（グレー化させない）。ハイライトは白飛びさせない
- WebGL 不可時: canvas 背景にカンプ PNG を表示するフォールバック

### 4.3 アニメーションクロック

- `elapsed`（秒）が唯一の時間源。`requestAnimationFrame` で加算、`params.running=false` で停止
- すべての動き（ドット周回・自転・ディザ・ゆらぎ・軌道ゆれ）が `elapsed` から計算される＝一時停止で全部止まる
- ドット角度: `patternDeg()` = 緩急ゆらぎ（サイン波の速度変調 `warpTime`）＋遅延・ランプ・個別スピード
- **デバッグ用フック** `window.__anim`: `setElapsed(t)` / `step(dt)` / `setSway(n)` / `setDesign(k)` / `setDither(n)` / `setLight(n)` / `getState()`
  - ⚠️ Claude の Browser プレビューペインでは rAF が発火しない。検証は `setElapsed()` で時間を進めてスクショ比較する

### 4.4 パラメーター

- `DEFAULTS` がすべての初期値。ユーザー操作は `localStorage`（キー `anyflow-embed-anim-v4`）に保存
- `load()` はデフォルトと deep-merge ＋ 削除済み選択肢のバリデーション
- 構造: `sway / design / dither / light / duration / globalSpeed / direction / ramp / pulse{amp,period} / sphere{duration,tumble,noise} / planet{dx,dy,scale} / orbits{outer,inner}{scale,angle,dx,dy,wobbleAmp,wobblePeriod,wobblePhase} / marquee{duration,direction} / dots[6]{speed,delay,offset,pulsePhase}`
- **軌道のゆれ位相は outer=0 / inner=180 がデフォルト** → 両方のゆれ幅を上げるだけで2本がクロスする

### 4.5 UI（2つのフローティングパネル）

1. **🎛 パターン切替スイッチャー**（初期位置 左下）
   - ヘッダーをドラッグで移動、右下つまみでリサイズ（CSS `resize: both`）、▲で全体開閉
   - アコーディオン式セクション: ゆらぎ（固定/シーソー/クロス）・惑星（B/C/D/E）・ライト（なし/右上光源）・ディザ（E選択時のみ表示: 固定/うつろい/流れ/さざ波）
   - 閉じたセクションは右側に現在の選択名を表示
   - **「📋 いまの設定をコピー」ボタン**: 現在の `params` を JSON でクリップボードへ。
     これを Claude に貼れば `DEFAULTS` に焼き込んで「全訪問者のデフォルト」にできる（デフォルト化ワークフロー）
2. **⚙️ アニメーション調整パネル**（右下）
   - 大カテゴリのアコーディオン: 🪐惑星（自転/位置・サイズ）・🛰軌道（外側/内側 個別）・⚪ドット（全体/緩急/各ドット）・🎞ロゴカルーセル
   - 下部に 一時停止 / 最初から / 初期値に戻す

## 5. 削除済み機能（git 履歴から復元可能）

ユーザー指示で 2026-08-13 に削減。必要なら該当コミットから復元する。

| 機能 | あったもの | 参考コミット |
|---|---|---|
| アニメ（ドット周回）5パターン | スタンダード/コメット/ドリフト/すれ違い/リズム | `4924d48` |
| ゆらぎ 11種 | ふわふわ/うねり/ジェリー/惑星ゆらり/逆位相/漂流/呼吸/波乗り など | `4924d48` `604f227` |
| 惑星A やわらかパステル | テクスチャ＋専用finish（assets/planet-a.png も削除） | `d4d92ee` |
| ディザ ⑤ざわめき | セル単位のチラつき | `e09a701` |
| グロウ系 ⑥波/⑦漂い/⑧鼓動 | ドットマトリクス表現（参考GIF再現→ブランド色化） | `633877e` `6f772f0` |

## 6. 作業の続け方（次の Claude への指示例）

1. 修正 → `index.html` を直接編集（単一ファイル）
2. ローカル確認 → launch.json の `anyflow-embed`（port 8773）。アニメ検証は `__anim.setElapsed()` を使う
3. コミット → main に直接。author email が `dosanko.design@gmail.com` であることを確認
4. `git push origin main`
5. **本番反映** → `cd anyflow-embed && npx vercel --prod --yes`（push だけでは反映されない）
6. `curl -sI https://anyflow-embed.vercel.app` で 200 を確認

### デフォルト化の手順（ユーザーから設定JSONが貼られたら）

スイッチャーの「設定をコピー」で得た JSON が貼られたら、その値を `index.html` の
`DEFAULTS`（および `DOTS` 初期角度はそのまま）へ反映する。
**パラメーターの「意味」を変えた時は `STORAGE_KEY` のバージョンも上げること**
（現在 **v39**。上げないと利用者のブラウザに残った旧値が新デフォルトを上書きして壊れる）。

## 7. スクロールセクション（KV以降・2026-08-13 実装）

絵コンテ（STORYBOARD-NOTES.md）とデザイン本体（Figma node `14693-28238`, TOP 1440×7425）を元に
KVより下の全セクションを実装した。ライブラリなし、素の sticky + スクロール進捗計算。

### 構成と仕組み

- **ピン留めセクション** (`.pin` > `.pin-vp`(sticky 100vh) > `.pin-stage`(1440×921を`--sp`でスケール))
  - `#vision` … Our Vision 文字出現→左スライド→ミニ楕円→文字退場→楕円がPlatform図へ移動拡大→
    ドット周回して停止→役割ラベル→「連携が生む、2つの価値」→Point 01/02
  - `#dev` … ダーク。見出しブラー出現→上下分割→間からモック拡大→ブラー切替→2巡目→
    モック右ドック→CLI/API/SDK切替（**ここは絵コンテで「Claude補完」指定 → 複数案あり**）
  - `#cases` … 導入事例。**スティッキースタック**（カンプ `14767:16333` 準拠）
- **通常フロー** `#results` … 画面に入ったらグラデ画像鮮明化＋カウンター（ランダム→確定、
  200+/500+/99.9%。1weekはカウントなし＝絵コンテ指定）
- 進捗計算は `pinP()`、フェーズ分割は `VIS_P` / `DEV_P` / `CASE_IN` 定数（0〜1の区間）
- すべて `renderFrame()` → `updateSections()` で毎フレーム適用（一時停止はカウンターにも効く）

### スクロールのリプレイはしない（2026-08-14）

- 各セクションの自動再生の開始時刻（`visAutoT0` / `resT0` / `devHoldT0` など）は
  **一度動き出したら null に戻さない**。スクロールで戻っても再生し直さず、終わった状態のまま
- リロード時は必ずKVの先頭から。`history.scrollRestoration = 'manual'` ＋ `scrollTo(0,0)`
  （ブラウザ既定の位置復元が効くと、途中から復元されて自動再生が中途半端に始まる）
- 頭から見直したい時は 調整パネルの「↺ 最初から」か `__anim.replay()`。
  どちらも `resetSectionClocks()` を呼ぶ。**新しい時間駆動アニメを足したら、この関数にも追加すること**

### ビジョンの進み方＝【出だしだけ自動、あとは全部スクロール】（2026-08-14 ヒデさん選択）

| | 何で進むか |
|---|---|
| 「Our Vison」＋1行目が出るまで（`VIS_AUTO_HEAD` = 2.05秒） | **時間**（到着した瞬間に反応が返るように） |
| 2行目 → 上下に分かれる → 軌道が出る → 左下へ → 2つの価値 | **ピン中のスクロールだけ**（手を止めれば絵も止まる） |

```
A = min(VIS_AUTO_HEAD, 経過秒) + clamp01(p / 0.88) × (VIS_TOTAL − VIS_AUTO_HEAD)
visT = max(前回値, A)     ← 巻き戻さない
```

⚠️ **経緯**: もともと全部が時間駆動 → 空スクロールの指摘で「時間とスクロールの速い方」に
→ それでも「時間で勝手に進む」と指摘されて現在の形へ。**`max()` に経過秒を混ぜ戻さないこと**。
`line2Gap` を 1.15 にしてあるのは、1行目が出きった直後にスクロール側へバトンを渡すため。

### 見出し「つなぐことが、強みになる時代へ。」の出し方（2026-08-14 作り替え）

**中央寄せ → その場で上下に分かれ、間から軌道グラフィックが出てくる**形。
（絵コンテ4コマ: ①中央にOur Vison＋1行目 ②2行そろう ③上下に分かれて間にミニ軌道
 ④全部ブラーで消えて軌道だけ残る）

- `.vis-label` / `.vis-line` は `left:0; right:0; text-align:center` の中央寄せ
- `splitGap`(分かれ始めるまで) / `splitDur`(分かれる時間) / `splitAmt`(開く量・片側92px)
- 「Our Vison」は分割と入れ替わりで消える（`(1 - spK)`）
- ミニ軌道は**2行のちょうど間・画面中央**。
  ⚠️ `.pf-wrap` は `top:127px` なので、楕円中心のステージ座標は `127 + 333.5 + ty`。
  ここを間違えると軌道が2行目に重なる（実際に踏んだ）
- 以降（軌道が左下へ移動＋実寸 → 2つの価値）は従来どおり

🗑 **廃止**: 中央→左へのスライドと、その4案（`VISION_SLIDES` / `SLIDE_EZ` /
`slideX1,2` / `slideStagger` / `charStagger` / `measureVisCenter`）。**復活させないこと**。

### （旧）見出しの左スライド 5案 ※廃止済み

`VISION_SLIDES` / `SLIDE_EZ` で定義。調整パネルの「📖 ビジョン › 左へのスライド（動きの種類）」で切替。

| | 名前 | 中身 |
|---|---|---|
| A | ①ぬるっと（既定） | 臨界減衰 `easeSlick`。そっと出て慣性でながーく止まる |
| B | ②吸着 | `easeOutBack`。わずかに行き過ぎて戻る |
| C | ③文字が流れる | 文字ごとに時間差（`charStagger`） |
| D | ④走ってブレる | `easeOutExpo` ＋ 速度に応じた `blur()` |

※「途中で止まってからピタッと詰める」2段階（旧⑤すっと→ぴたっ）は
2026-08-14 に「いらない」との指示で削除済み。**再追加しないこと**。

**2行目は1行目が動き出したらすぐ追従する**（`slideStagger` 既定 6%。以前は45%で明らかに遅れていた）。

### 複数案（スイッチャーで切替）

- 開発者体験の切替: **案A ブラースワップ**(デフォルト) / 案B 縦送り / 案C ズーム切替
- 導入事例カード: **案A 奥行きスタック**(デフォルト・絵コンテ準拠) / 案B 順番に表示(カンプ準拠の縦並び)
- 調整パネルに「🎬 セクション演出」カテゴリ追加（各セクションのスクロール尺・ブラー量・
  カウント時間・奥行き強さ）。パラメーターは `params.sections` / `params.patterns`

### 導入事例のスティッキースタック（2026-08-14 作り直し）

カードは**カンプのように縦に gap 40px 空けて並んだ状態**で出てきて、
スクロールするとリストごと上へ流れ、上端に着いたものから順に貼り付いて重なる
（2026-08-14: 画面外から1枚ずつ飛び込む形をやめた）。

終着点は **3枚とも上端 172（＝「導入事例」の上っ面）にぴったり重なった状態**
（2026-08-14 ヒデさん指示: 「完全に全部3つ重なる／位置としては同じ位置で終わってほしい」）。
奥へ送られるたびに 0.1 ずつ縮小するので、奥のカードは手前のカードの内側に隠れる。

| | 上端 | 幅 | 縮小率 |
|---|---|---|---|
| 1枚目 SmartHR（最奥） | 172 | 704 | 0.80 |
| 2枚目 deex | 172 | 792 | 0.90 |
| 3枚目 SALES GO（最前） | 172 | 880 | 1.00 |

3枚とも**左右中央そろえ**（前面カードの右端が余白120px）。
※カンプ `14767:16333` は 172 / 251.8 / 366.1 と段差のある重なりだった。
段差を戻したい時は調整パネルの「重なりのズレ」を上げる（0 = 完全に重なる）。

動きは `arr`（何枚目まで積み上がったか 0→2）1本で決まる。
1枚目はその場でブラー出現、2枚目以降は画面下から上がってきて所定の位置に貼り付き、
新しいカードが着地するたびに前のカードが縮んで奥へ下がる。

| 名前 | キー | 既定 | 意味 |
|---|---|---|---|
| 出現ブラー | `cases.inBlur` | 20px | カードが出てくる間のぼけ量 |
| 奥行きの強さ | `cases.depth` | 100% | 1段につき縮小10%（100%で 0.8/0.9/1.0） |
| カードの間隔 | `cases.gap` | **40px** | 並んでいる時のカード同士の縦の間隔 |

**やめたこと**（2026-08-13〜14 に入れていたが、カンプに無い動きだった）:
3枚を縮小して縦に並べる状態 / 重なる時に実寸へズームする動き / 出現の自動再生（時間駆動）。
これらが「デフォルトの見た目もスクロール中の挙動もおかしい」原因だった。

⚠️ 位置とサイズは**画面上の見え方で補間してからリストの縮小率で割り戻している**。
素直にリスト座標で補間すると、拡大中に3枚目が一度画面下へはみ出してから戻る動きになる。

### 実装上の判断・注意

- **開発者モックはHTML/CSS製**（`.dm-panel`、コード行は `DM_ROWS` 定数から生成）。
  Figmaの画像書き出しはガラス半透明が白く焼き付くため画像は不採用（assets/dev-mock.png は参考用に残置）
- Platform図の楕円は Figma SVG書き出しの実数値（同一楕円 rx473.8/ry158.9 を -23.33° と -5.37° の2枚重ね、
  グラデはKVと同系）。ドット停止位置はカンプ座標から楕円式で逆算した角度（`PF_DOTS`）
- **線の太さはカンプの `stroke-width: 3`**（2026-08-14 に4→3へ修正）
- **Platform図の文字はカンプが字間を広く取っている**。カンプのテキスト幅から逆算して
  `.pf-name` / `.pf-sub` に `letter-spacing: 0.178em`、`.pf-title` に `0.0588em` を入れてある
  （検算: コネクタ=65.98px / プラットフォーム=150.8px / Platform=192px になれば正しい）
- **Point 02 の数字だけ水色 `#0EBBFF`**（Point 01 はピンク。■はどちらもピンク）
- 🚨 **Platform図でハマった2つの罠（2026-08-14 修正）**。どちらも
  「ウィンドウが 1440×921 ちょうどの時だけ正しく見える」ので検証をすり抜けた。
  **必ず 1512×830 など別サイズでも確認すること**
  1. 軌道の横位置を「2つの価値」の左端から逆算していた
     （`txFinal = valHeadLeft - 31*kFill - R*scFinal`）。
     カンプは軌道の原点がページ左端で左へ見切れる形なので、**`txFinal = 0` が正しい**
  2. 楕円とドットは `mapX/mapY` で拡大していたのに、
     **`.pf-texts`（Platform・プラットフォーム・役割名）は素のCSS位置のままだった**ので
     ウィンドウ幅によって文字だけ取り残されてズレた。
     いまは `.pf-texts` に `translate(tx, …) scale(sc)` を毎フレーム当てて軌道と一体化させている
- 事例カード写真: assets/case-smarthr.png / case-deex.png / case-salesgo.png（2倍解像度）
- 実績グラデ画像: assets/results-gradient.png
- **Strengthラベルは全部 "Strength 01"**（カンプ・絵コンテともにそうなっていたため踏襲）
- **実績セクションの余白**: カンプでは実績のかたまり（画像上端〜下段の数字まで **623.9px**）の
  上下に大きな余白がある（上 約420px / 下 約270px）。ただしビューポートに固定する以上
  450+624+450 = 1524px は 921px のステージに入らないので、
  **921pxの中で上下 148.5px ずつの均等余白になるよう中央配置**している（`top: 148.56px` 起点）。
  文字どおりの450pxを出したい場合は、このセクションをピン留めせず通常フローに戻すしかない
- 🚨 **「無駄なスクロール」が出たら、原因はほぼこの2つ**（2026-08-14 に何度も踏んだ）:
  ①セクション間の乗り換え区間（下記）が空っぽ ②演出が終わってから抜けるまでの尾が長い。
  **どちらも「その区間で何かが動いているか」を実測して潰す**。
  実績は `preP` で助走の3割ぶんを物語に使い、尺も 300→**220vh** に短縮した
  （出そろってから抜けるまで 0.26画面）。
  導入事例は「導入事例」の文字を**助走に入った直後（`seg(pre, 0.02, 0.45)`）**から出すので、
  黒いセクションを抜けた 52px 後にはもう文字が見えていて、真っさらな画面を挟まない
- 🚨 **ピン留めセクションの間には、必ず1画面ぶんの「乗り換え区間」がある**（2026-08-14 発覚）。
  sticky の仕組み上、前のセクションの固定が外れてから次が固定され始めるまでに
  ちょうど 100vh のスクロールが挟まる（前の画面が上へ抜け、次の画面が下から入ってくる区間）。
  **両端が空っぽ／同じ背景色だと、まるまる1画面ぶん「何も起きない空スクロール」に見える**。
  開発者体験→導入事例がこれで、白くなってから「導入事例」が出るまで 1.58 画面かかっていた。
  対策として `preP(sec)`（画面下から入ってきた割合 0→1）を用意し、
  導入事例のラベルと1枚目はこの**助走区間のうちに出しきる**ようにした（いまは 0.92 画面）。
  他のセクション境界でも間延びを感じたら同じ手を使う。
  デバッグは `__anim.setProgress('casesPre', 0〜1)`
- 🚨 **ピン留めセクションを「時間だけ」で再生させないこと**（2026-08-14 に実績で発覚）。
  ピンしている間にスクロールしても何も進まず、再生が終わってから
  さらに数画面ぶん空スクロールしないと下へ抜けられない＝「無駄なスクロール」になる。
  実績は `resT = max(前回値, 経過時間, (p/0.8) * RES_TOTAL)` の**時間とスクロールの速い方**方式にした。
  止まっていても勝手に出てくるし、スクロールした分はちゃんと進み、巻き戻りもしない
- 開発者体験の間合い（2026-08-14 に「短絡的」との指摘で緩めた）:
  モックが右へ寄る `dock` を 6%→**12%** かつイージングを `easeSlick`（ぬるっと）に、
  CLI/API/SDK の入れ替え `sw1`/`sw2` を 3%→**8%** に。
  入れ替えのブラーは `swapDip()`（`sin` の 0.7 乗で山を丸くしたもの）。
  文字は `1 - dip*1.35` でいったん**完全に消えてから**次が出る
  （薄いまま入れ替わると同じ文字のクロスフェードに見えて安っぽい）。モックは消さず 0.6 倍のぼけだけ
- KV（ヒーロー）は既存実装のまま。※新カンプではKV見出しが「AIと事業を強くする」+
  小ラベル「APIでデータを繋ぐプラットフォーム」に変わっているが、指示があるまで未反映
- カンプにフッターは存在しない（SALES GOカードでページが終わる）
- デバッグ: `__anim.setProgress('vision'|'dev'|'cases', 0〜1)` でスクロールに関係なくフェーズ固定
  （解除は null）。`__anim.startResults()` + `__anim.step(dt)` でカウンター確認。
  Claudeのプレビューはスクロールすると描画が固まるので、検証時は対象の `.pin-vp` を一時的に
  `position:fixed; inset:0` にして setProgress で回すのが確実
- launch.json の anyflow-embed は autoPort 対応済み（8773が他セッションに取られていても起動する）

## 7.5 スマホ対応（2026-08-14 / まずは一次対応）

**PCの 1440×921 をそのまま縮めない**。390px幅で縮めると本文が4pxになって読めないため、
**設計フレームごと 390×780 に差し替える**方式にしてある。

- `fit()` で `w <= MOBILE_MAX(780)` なら `DW=390 / DH=780`、`<html>` に `.mb` が付く
- 高さは CSS 変数 `--dh` で全ステージに配る（`.flow-stage` / `.pin-stage` / `#results .pin-stage`）
- **CSS は `@media (max-width: 780px)` の1ブロックに集約**。ここに書く数値は 390px幅の実寸そのもの
- JSでジオメトリを計算している所はスマホ分岐がある:
  | 対象 | スマホでの扱い |
  |---|---|
  | KVの軌道グループ | JSの transform に `scale(0.40)`。CSSで原点を左上・左寄せに |
  | ビジョンの軌道図 | `kFill` の基準幅を 1440 → **900** に（画面幅いっぱいに見せる） |
  | 図の中の文字 | 図が縮むぶん **CSSで大きめのfont-size**を置いて実効サイズを稼ぐ |
  | 開発者体験のモック | `mockK()=0.40` で縮小。**右へドックせず中央のまま**（`mockCenterX()` が 0 を返す） |
  | 導入事例のカード | **写真が上・文章が下の縦組み**に変更。`caseH()=372 / caseTop()=118` |
- 見出しの `top` は HTML の inline style なので、`#devH1 / #devH2` は `!important` で上書きしている
- ⚠️ 780px以下は全部このスマホ用レイアウト。タブレット横（1024px前後）はPCレイアウトが
  55〜70%に縮んだ状態になる。気になるなら中間ブレークポイントを足す


## 7.6 参考36サイトの実測から入れた「動きの共通ルール」（2026-08-14）

Eagle『プロジェクト > Anyflow > Rebranding > 参考サイト』36サイトを全件ブラウザ実測して反映した。
計測メモの要点:

- **Lenis(慣性スクロール)採用率 21/36 = 58%**。ただし**日本のサイト5件は全部Lenisなし**で、
  CSSトランジションの秒数とカーブだけで質感を作っていた
- **固定(sticky)が2画面を超えるサイトは1つも無かった**（最長の sakazuki.io は全長30画面だが
  固定は1〜2画面ぶんずつ）
- いちばん多かったカーブは **`cubic-bezier(.22,1,.36,1)`（easeOutQuint）**

### 動きのトークン（CSSとJSで同じ値を持つ）

| 用途 | CSS | JS | カーブ |
|---|---|---|---|
| 小さい反応(ホバー等) | `--t-fast: .25s` | `T_FAST` | `--e-fast` = `cubic-bezier(.4,0,.2,1)` |
| 出現 | `--t-in: .8s` | `T_IN` | `--e-out` = `cubic-bezier(.22,1,.36,1)` |
| 大きい移動 | `--t-big: 1.3s` | `T_BIG` | 同上 / JSは `easeSlick` |

**`easeOutQ`（JS）と `cubic-bezier(.22,1,.36,1)`（CSS）は同じカーブ**。新しく動きを足す時は
この3段階のどれかに必ず寄せること（以前は 0.8/0.9/1.4/1.5/1.9秒がバラバラに散っていた）。

### 慣性スクロール (Lenis)

- `assets/lenis.min.js` に**自前ホスト**（CDN依存にしない）。`frame()` の先頭で `lenis.raf(ts)`
- `duration: 1.1` / easing は easeOutQuint（トークンと同じカーブ）
- **スマホは `syncTouch: false`** で端末標準の慣性に任せる
- OSで「視差効果を減らす」にしている人には掛けない
- ⚠️ **`html { scroll-behavior: smooth }` は入れないこと**（Lenisと喧嘩する）
- ヘッダーのアンカーリンクは `lenis.scrollTo()` 経由に差し替え済み

### 見出しの1文字ずつ出し

`vision.charLag`（既定 0.5）。0 にすると従来の行まるごと出しに戻る。
⚠️ 案C（文字ごと横流し）と**同じ `style.transform` を取り合う**ので、
縦の出現と横のスライドは**1回の代入にまとめて**当てている。別々に書くと後勝ちで消える。


### 🚨 空スクロールの見つけ方と潰し方（2026-08-14 に全セクションで実施）

「所定の位置に来たのに何度もスクロールしないと進まない」は、原因が必ずこの2つのどちらか。

1. **セクションが時間駆動だけで動いている** → ピン中にスクロールしても進捗が1ミリも動かない。
   ビジョンがこれだった（1.6画面ぶん完全な無反応）。実績も同じ穴があった。
   → **`max(前回値, 経過時間, スクロール由来の時間)`** にして「速い方で進む」形にする。
     止まっていても勝手に再生され、スクロールした分はちゃんと進み、巻き戻りもしない
2. **乗り換え区間(必ず1画面)が空っぽ** → `preP(sec)` で助走ぶんを進捗の頭に繰り込む。
   開発者体験は `PRE = 0.12` として、1つめの訴求文が助走のうちに出るようにした

**確認方法**（実測したコード。困ったらこれを回す）:
```
const yPin=(s,p)=>s.offsetTop+p*(s.offsetHeight-innerHeight);
const yPre=(s,q)=>s.offsetTop-innerHeight+q*innerHeight;
// 「前セクションが出そろう位置」と「次セクションが動き出す位置」の差を画面数で見る
```
2026-08-14 時点の実測: ビジョン完了→実績開始 **0.27画面** / 実績完了→開発開始 **0.19画面** /
白くなり切る→導入事例の文字 **0.06画面**。**0.3画面を超えたら要調整**。

### 調整パネル（2026-08-14 に作り直し）

- **外寸を固定**（既定 360×520px）して、中身だけ `.panel-body` でスクロールさせる。
  ⚠️ 高さを内容で決めると、アコーディオンを開くたびにパネルが伸び縮みして
  **触っている場所がズレる**（レイアウトシフト）。実際に苦情が出た
- `.panel-body { overscroll-behavior: contain }` でページへスクロールが伝播しないように
- ⚠️ **Lenis に `prevent` を渡さないと、パネル内のホイールをページに全部持っていかれる**
  → `prevent: node => !!node.closest('.tools')`
- ヘッダーを掴んでドラッグで移動（`.tools` を left/top 指定に切り替える）。
  右下つまみでリサイズ（`resize: both`）。ドラッグした時は開閉トグルを発火させない


### フォントウェイト一覧（カンプ `14693:28238` と全件突き合わせ済み・2026-08-14）

Figma の指定を `get_design_context` で取って実測と照合した。**新しく文字を足す時はこの表に合わせる。**

| 箇所 | 書体 | ウェイト | サイズ |
|---|---|---|---|
| KV 小ラベル | Noto Sans JP Regular | 400 | 14px |
| KV 見出し | Noto Sans JP Thin | 100 | 70px / lh1.4 |
| Our Vison | SF Pro Text Light | 300 | 40px |
| つなぐことが、 | Noto Sans JP Thin | 100 | 100px / lh1.7 |
| 連携が生む、 | Noto Sans JP Regular | 400 | 20px |
| 2つの価値 | Noto Sans JP Thin | 100 | 60px |
| Point 01 | SF Pro Text Light | 300 | 20px |
| 賢いAIの土台をつくる | Noto Sans JP Light | 300 | 26px |
| Point 本文 | Noto Sans JP Light | 300 | 14px |
| 事業の推進力を生み出す | Noto Sans JP **ExtraLight** | **200** | 40px |
| 導入企業 | Noto Sans JP Regular | 400 | 16px / lh24 |
| 200+ | SF Pro Text **Ultralight** | **100** | 120px |
| 連携実績 / 稼働率 / 最短リリース | Noto Sans JP Light | 300 | 16px / lh24 |
| 500+ / 99.9% / 1week | SF Pro Text **Thin** | **200** | 50px |
| 導入事例 | Noto Sans JP **ExtraLight** | **200** | 44px |
| カードの引用文 | Noto Sans JP Light | 300 | **22px** |
| カードのタグ | Noto Sans JP Light | 300 | **10px**（枠 **0.5px 黒**） |
| カードの会社名 | Noto Sans JP Light | 300 | **24px** |
| Strength 01 | SF Pro Text **Thin** | **200** | 20px / 字間 **-1px** / **#0EBBFF** |
| SDKへの対応 など | Noto Sans JP Light | 300 | **34px** |

**2026-08-14 に直した8箇所**（太字が修正後）:
事業の推進力(300→**200**) / 導入事例(100→**200**) / カード引用文(400・24px→**300・22px**) /
カードタグ(12px・1px#c9c9c9・#555→**10px・0.5px黒・黒**) / カード社名(26→**24px**) /
Strength 01(Noto300・16px・#00ABEB→**SF Pro Thin200・20px・字間-1px・#0EBBFF**) /
訴求文(200・36px→**300・34px**) / 大見出し側の Strength ラベルも同書式にそろえた。
カード間隔も 40 → **44px**（カンプ `14708:37827` の gap）。


### 🚨 カンプ照合でハマった落とし穴（2026-08-14・再発防止）

**① 「テキスト枠の幅」から字間を逆算してはいけない。**
`get_metadata` はテキストの**枠のサイズ**しか返さない。Figma のテキスト枠は
デザイナーが手で広げていることが多く、文字送りとは無関係。
これを字間と誤読して `.pf-name` に `letter-spacing: 0.178em` を入れてしまい、
カンプより間延びした文字になっていた（2026-08-14 撤去）。
**フォントの指定は必ず `get_design_context` で読む**（書体名・ウェイト・サイズが文字列で返る）。

**② カンプは更新される。**
2026-08-14 の照合で、Platform図に**「認証基盤」ラベルが追加**され
**コネクタの位置も変わっていた**ことが判明（ノードID `14779:*` が新しく振られていた）。
実装が古いカンプのままになっていた。**照合の時は「カンプにあって実装に無い要素」も必ず数える。**

**③ 一部だけ照合すると穴が残る。**
最初の照合では主要19箇所を見たが、Platform図のラベル群が対象から漏れていた。
**セクション単位で全テキストノードを列挙してから潰すこと。**

### Platform図のテキスト（カンプ 14693:28351 実測・字間なし）

| | 書体 | ウェイト | サイズ |
|---|---|---|---|
| Platform | SF Pro Text Thin | 200 | 50px（192px幅の箱で中央そろえ） |
| プラットフォーム | Noto Sans JP Light | 300 | 16px（Platform の 9px 下） |
| コネクタ/認証基盤/ワークフロー/権限/監査 | Noto Sans JP Light | 300 | 14px |

ラベル位置（`.pf-wrap` ローカル座標 = カンプ frame y − 1851.98）:
コネクタ(226.03, 226.82) / 認証基盤(707.19, 59.10・**中央そろえ**) /
ワークフロー(700.37, 329.84) / 権限(548.87, 430.34) / 監査(202.16, 560.82)


### 🚨 英字は「SF Pro **Text**」。Display と間違えないこと（2026-08-14）

カンプの英字は **全部 SF Pro Text** 指定（Our Vison / Point 01 / Platform / Strength 01 /
200+ / 500+ / 99.9% / 1week）。SF Pro には **Text と Display の2つの光学サイズ**があり、
**Display は字幅が狭く線も細い**。`--font-en` に Display を先に書いていたため macOS が
Display を選んでいて、実測で「200+」が **277.4px（正しくは 290.6px）と約5%詰まっていた**。

`--font-en` は **`'SF Pro Text'` を先頭**に置く。未インストール環境は `-apple-system` に落ちる。

**書体が合っているかの確かめ方**（目視で判断しないこと）:
```js
const cv = document.createElement('canvas').getContext('2d');
const w = (fam) => { cv.font = `100 120px ${fam}`; return cv.measureText('200+').width; };
w(getComputedStyle(document.documentElement).getPropertyValue('--font-en'))  // 実際に使われる書体
w("'SF Pro Text'")     // カンプの指定 → この値と一致すれば正しい
w("'SF Pro Display'")  // 間違い側
```


### スクロールの勢いで再生スピードを変えない（ページ全体共通・2026-08-14）

ホイールを強く回した時だけ演出が早送りになると、ゆったりした体験が壊れる。
`smoothTo()` に **1秒あたりに進める進捗の上限**（`sections.common.maxRate`、既定 0.35
＝1セクションを渡り切るのに最短2.9秒）を入れて、それ以上は速くならないようにしてある。
**助走(Pre)側にも同じ上限をかける**ので `smoothP` はセクションごとに2つ持つ
（`vision` と `visionPre` など）。調整パネルの「🌊 全体 › 再生スピードの上限」で変えられる。

⚠️ 新しくスクロール駆動の値を足す時は、**必ず `smoothTo()` を通すこと**。
生の `pinP()` / `preP()` を直接使うと、そこだけ勢いで早送りになる。

### キービジュアルの出す順番（2026-08-14 変更）

ヘッダー → **タイピング（AIと事業を／強くする）** → **小ラベル「— APIでデータを繋ぐ
プラットフォーム」が罫線ごとブラー出現** → 軌道グラフィック → ロゴ帯。
小ラベルは `eyebrowGap`（打ち終わりからの秒数）で位置が決まる。以前は先に出ていた。

### ビジョンの開始タイミング（2026-08-14 変更）

**セクションがビューポートの中央に来てから**（`pre >= 0.5`）自動パートが始まる。
以前は画面下端に顔を出した瞬間（`pre > 0.002`）で、早すぎるとの指摘があった。


### 惑星の配色は「カンプの面積比」で持つ（2026-08-14）

`assets/planet-e.png`（カンプの惑星）のピクセルを数えた**実測の面積比**:

| 色 | 面積 |
|---|---|
| 水色/シアン | **55.7%** |
| 青 | 24.2% |
| 白に近い | 8.3% |
| マゼンタ/ピンク | **7.5%** |
| 紫 | 4.1% |

`waveFragment()` の `ramp()` は、**ストップの幅をこの比率に合わせてある**
（ピンク0.075 / 紫0.115 / 青0.355 / 水色0.915 / 白1.0）。
⚠️ **等間隔のストップにするとピンクと紫が出過ぎて色が汚くなる。** 実際にそうなって指摘を受けた。
色の値もカンプ実測（#FF5D97 / #8075DC / #1AA8FB / #0BDDFF / #EAFFFF）。

**測り方**（色を足す・変える時はこれで比率を確認する）:
```
convert assets/planet-e.png -resize 160x160! -depth 8 txt:- で生RGBを取り、
球の内側だけ抜き出して色相で分類 → 面積%を出す
```


### スクロール＝きっかけ / 再生＝時間（2026-08-14 採用・全セクション）

**スクロール位置は「ここまで再生してよい」という上限だけを決める。絵を進めるのは時間。**
だから手の速さで再生スピードが変わらない。`gateP(key, target, rate)` が本体。

```js
storyP[key] = Math.max(prev, Math.min(target, prev + frameDt * rate));
```

| 操作 | 起きること |
|---|---|
| 勢いよく回す | 上限だけ先へ飛ぶ。絵は等速で追う → **早送りにならない** |
| 少し回す | そのぶんだけ再生して待つ → **見せたい所で見てもらえる** |
| 止める | 上限まで再生して停止。続きはスクロールで解放 |

実測: ビジョンで一気に最後までスクロールしても **12秒かけて等速再生**。
p=0.3 で20秒待っても途中で停止し、p=1 に解放すると続きが流れる。

`rate` は「1秒あたり進む進捗」。ビジョン/実績は秒の世界なので 1（1秒/秒）。
開発者体験 0.085・導入事例 0.16（調整パネルの「再生スピード」で変えられる）。

🚫 **やってはいけない**: 進捗そのものに速度上限（旧 `maxRate`）をかける方式。
スクロールしても画面が動かず“のっそり”して不採用になった。**上限を決めるのはスクロール、
進めるのは時間**という役割分担が肝。

### Platform図の役割ラベルは「ドット基準」で持つ

`PF_LABELS` は絶対座標ではなく `{ dot, dx, dy }`（ドット中心からのズレ）。
⚠️ 絶対座標だと、ドットの角度を直す・カンプが更新される・拡大率が変わる、の
どれが起きてもラベルだけ取り残される（**実際に2回踏んだ**）。
実測でカンプの相対値と小数第1位まで一致することを確認済み。
※ カンプのドットは楕円の線から最大7.5px手でずらして置かれているが、
ドット基準なのでその影響を受けない。

⚠️ `VIS_TOTAL` は **ドットの周回が止まるまで**（自動5.275＋移動2.0＋周回6.5）を含めること。
短いとドットが回りっぱなしになる（12.6秒に修正済み）。


### 🎬 所定の位置で止めて、1本の映像として自動再生（2026-08-14 採用・現行方式）

**各セクションは「固定位置に着いたら自動再生される映像」。再生中はスクロールを止め、
終わったら解放する。** スクロールは再生位置に一切関与しない（＝手の速さで速度が変わらない）。

| 仕組み | 実体 |
|---|---|
| 再生 | `playT(key, total, canStart)` … 固定した瞬間に `t0` を記録し、以降 `elapsed - t0` |
| ロック | `updateScrollLock()` を毎フレーム。再生中は `lenis.stop()`、終わったら `start()` |
| 閉じ込め防止 | 再生中にホイールを回すと**最大4倍速で早送り**（`fastForward()`）。手を止めると数百msで元の速さへ戻る |
| 検証 | `__anim.lockState()` で `{locked, playing, sections}` が見える |

**セクションの再生時間**（調整パネルで変更可）:
ビジョン `VIS_TOTAL` **8.4秒** / 実績 `RES_TOTAL` **5.4秒** /
開発者体験 `dev.playSec` 16秒 / 導入事例 `cases.playSec` 8秒

**ビジョンの実測タイムライン（2026-08-14 に間延びを詰めた後）**:
2行目そろう 1.73s → 上下に分かれ始め 2.27s → 軌道が出そろう 3.23s →
**ドット停止＆Platform 5.92s** → 役割ラベル 6.80s → Point02 8.20s → 解放 8.40s

⚠️ **ドットの周回は絵コンテに「1秒ぐらいでいいかな」と明記がある**。
`spinDur 4 + spinBrake 2.5 = 6.5秒`になっていて、ここだけで7秒待たされていた
（→ 1.0 + 1.2 = 2.2秒に修正）。尺が伸びたら、まずここを疑う。

**実績の実測**: 画像 0.80s → 見出し 1.22s → 200+ 1.62s → 下段そろう 3.20s →
カウンター確定 4.45s → 解放 5.40s

**ビジョンの順番（ヒデさん指定）**: メッセージ → 上下に分かれる → 軌道が出る →
軌道が左下へ＋**ドットが周回** → **ドットが止まってから** Platform・プラットフォーム・
役割ラベルがブラー出現 → 2つの価値 → Point 01 → Point 02 → 解放。
⚠️ 文字チェーンの起点は `mvT - (cruise + brake)`。**`moveDur` 起点にするとドットが
まだ回っている最中に文字が出る**（実際に踏んだ）。

⚠️ **スマホは Lenis がタッチを握っていない**（`syncTouch: false`）ため**ロックが効かない**。
その場合は「時間で再生されるが、スクロールは通る」動作になる。

🚫 **過去に試して失敗した方式**（戻さないこと）:
① 進捗＝スクロール位置に直結 → 手の速さで**早送り**になる
② 進捗に速度上限(`maxRate`) → スクロールしても動かず**のっそり**
③ スクロールが上限を決めて時間で追う(`gateP`) → 手を止めると絵も止まり、
   開発者体験・導入事例が**のっそり**すると指摘（`gateP` は未使用のまま残置）


### 🚨 アニメが途中で固まる／カクつく時の2大原因（2026-08-14 修正）

**① 描画ループが例外1回で永久停止していた**
`frame()` の中で例外が出ると `requestAnimationFrame(frame)` の再予約に到達できず、
**全アニメーションがその場で凍りつく**（画面が中途半端な絵で固まる）。
→ 本体を `try/catch` で包み、**次フレームの予約を try の外**に出した。
例外は `console.error('[anyflow] 描画中の例外:', e)` で1回だけ出る。
⚠️ 新しい描画処理を足す時も、**再予約を条件分岐の中に入れない**こと。

**② 所定の位置に着く前にスクロールをロックしていた**
再生開始の条件が `p > 0.002`（＝固定が始まったら何でも）だったため、
勢いよくスクロールするとセクションが**行き過ぎた位置でロックされ、そこで固まって見えた**。
→ `playT()` に対象セクションを渡し、**上端から6px以上ずれていたら
`lenis.scrollTo(sec.offsetTop, { lock: true })` で所定の位置へ寄せてから**再生を始める。

**調べ方**（同じ症状が出たらまずこれ）:
```js
__anim.lockState()   // { locked, playing, sections } が見える
// 全セクション×進捗を総当たりして例外が出ないか確認する probe も有効
```


### 🚨 「途中からいきなり真っ白」の原因（2026-08-14 修正）

閉じ込め防止の早送りが **`skip += 999` で残り全部を飛ばす**実装だった。
開発者体験は**最終フレームが白背景**なので、再生中にスクロールすると
**いきなり最終フレーム＝真っ白にワープ**していた。
→ `fastForward()` で「回している間だけ最大4倍速」に変更。**飛ばずに早く流れるだけ**。
実測: ホイール40回で 1.84秒ぶん進むだけ／白は 0.00 のまま。

### 🚨 ビジョンのカクつき（2026-08-14 修正）

助走（Our Vison と1行目）を見ずに本編へ入ると、`A` が 0 → `VIS_AUTO_HEAD`(1.35秒) へ
**時間が飛んで**、1行目が一瞬で出ていた。
→ `A = max(本編, 助走で進んだ分)` にして、どちらの入り方でも時間が飛ばないようにした。
実測: 助走なしで固定に入った直後の1行目は **0文字**（飛んでいない）。

⚠️ 所定の位置へ寄せる `scrollTo` が途中で中断されると `onComplete` が来ず、
**永久に再生が始まらない**恐れがあるため、**1.2秒で諦めてその場で再生開始**する保険を入れてある。


## 🎬 開発者体験は「章立て」（2026-08-14）

1本16秒の通し再生だと、**途中でスクロールされて先へ行かれてしまう**（アニメが終わって
いないのに次の画面に進む）ため、**5つの章**に分けた。1章ずつ

**再生（スクロールをロック） → 終わったら解放 → スクロールすると次の章が再生**

を繰り返す。実装は `chapterP(key, DEV_CH, pScroll, canStart, sec)`。

| 章 | 描く範囲 (p) | 尺 | 次章を解放するスクロール量 | 章の終わりの絵 |
|---|---|---|---|---|
| ① | 0.00 → 0.22 | 4.2秒 | — | 「自動生成で開発スピードを加速」＋モックが静止 |
| ② | 0.22 → 0.60 | 5.0秒 | 0.20 | 「開発環境に柔軟に適用」＋モックが実寸 |
| ③ | 0.60 → 0.79 | 3.2秒 | 0.42 | モックが右へ、左に「CLIへの対応」 |
| ④ | 0.79 → 0.88 | 2.2秒 | 0.64 | 「APIへの対応」 |
| ⑤ | 0.88 → 1.00 | 2.8秒 | 0.86 | 「SDKへの対応」→ 白へ |

- **章の区切りは必ず「絵が完成して静止している所」に置く。** 退場や切り替えの
  途中で解放すると、ユーザーは何も無い画面を見ながら待つことになる
- 章と章の間は `secPlay[key].t0 === null` → `anyPlaying()` が null → **ロックしない**
- `dev.lenVh` は 850 → **300** に短縮。尺は「章を送るためのスクロール量」なので短くてよい
  （1章あたり約40vh のスクロールで次へ）


## 🎬 開発者体験（2026-08-14 カンプ 14791:29575 で全面刷新）

**旧レイアウト（見出しが上下に割れて、モックが右へドックし、左に CLI/API/SDK の文章）は廃止。**

| | カンプ実測値 | 実装 |
|---|---|---|
| 見出し枠 | 上端 143.8px・中央揃え・gap 12px | ✅ |
| ラベル「Strength 02」 | SF Pro Text **Thin(100)** 20px / 字間 -1px / 行間 1 / #0EBBFF | ✅ 100 / 20px / -1px / 20px |
| 見出し | Noto Sans JP **ExtraLight(200)** 44px / 行間 normal / 白 | ✅ 200 / 44px |
| モック | 左 79.7px・上 315px・840.6×504.37 | ✅ 左79.7 幅840.6 |
| 右リスト枠 | 左 980.3px・上 315px・幅 329.383px・上余白20・項目間 37px・右寄せ | ✅ 左980.3 幅329.4 間隔37.0 |
| 「API」 | SF Pro Text **Thin(100)** 28px | ✅ |
| 「の場合」 | Noto Sans JP **Light(300)** 18px | ✅ |
| 本文 | Noto Sans JP Light(300) 12px / 行間 1.9 | ✅ 22.8px |
| 選択中の縦線 | 1px 白・高さ115px・本文との間 28px | ✅ 114.9px |
| 非選択 | **不透明度 0.2**（カンプ実測） | ✅ |

⚠️ 旧実装はラベルを font-weight **200** で書いていたが、カンプは **Thin=100**。今回 100 に修正した。

### 章立て（2章）

| 章 | 中身 | 尺 | 解放スクロール |
|---|---|---|---|
| ① | モックが下からスケールアップ → 「Strength 01 / 自動生成で開発スピードを加速」→ 中身のアニメ | 6.0秒 | — |
| ② | ①退場 → モックが出直す → カンプ位置(左)へ移動 → 「Strength 02」→ 右リストが順に出る | 8.4秒 | 0.42 |

- **⚠️ 章の終わり `to` は、退場の開始点 `out1[0]=0.42` より必ず手前に置くこと。**
  同じ値にすると、章の最後のフレームで退場側の描き方に落ちて **絵が消える**（実際に踏んだ）
- 白背景へ戻すのは章の進捗 `p` ではなく **生のスクロール `pRaw`（DEV_FIN）**。
  `p` に紐づけると、出したばかりのリストが即座に白飛びする

### ホバーで画面が切り替わる（②の後）

右リストをホバー／クリックすると、左のモックが**ブラーで**切り替わる。既定は **API**（カンプの選択状態）。

| 選択 | モックの見た目 |
|---|---|
| API | エディタ＋AIペイン（カンプ通り）。コードは HTTP リクエストとレスポンス |
| CLI | **ターミナルだけ**（`.is-term` でサイドバーとAIペインを外し、mac風のタイトルバーを出す） |
| SDK | 右ペインを **SDK Reference 一覧**に差し替え（角丸・余白はAIペインを踏襲、中身だけ別物） |

### スキップ動線（カンプには無い・ユーザビリティのため追加）

モックの**上下**に「スキップして次へ ›」というテキストリンク。再生開始から1秒後に静かに出る。
押すと `devSkip()` が**いまの章だけ**終わらせ、次の章の位置までスクロールする。
⚠️ 全体を飛ばしてはいけない（最終フレーム＝白へワープするため）。

## 🌀 ロゴティッカー（2026-08-14 シームレス化）

以前は帯画像 `logos-strip.png` 1枚を2回並べていた。画像内のロゴ間隔は **91〜134px とバラバラ**で、
継ぎ目の間隔は **242px**。だからループのつなぎ目に穴が空いて見えていた。
→ 実測したインクの範囲で **ロゴを7枚に切り出し**（`assets/logo-1..7.png`）、
`.mq-set { gap: X; padding-right: X }` で**どこで切っても間隔が同じ**にした。実測 全間隔 96px。

## 🌍 Our Vision のドット（2026-08-14 順序変更）

**軌道がブラーで出た時点でもうドットは回っている** → 移動 → Platform → 2つの価値 を見せている間ずっと
等速で回り続け、**最後に減速して止まってから**役割名（コネクタ等）が出る。

実測: ドット出現 → Platform(+3.1s) → 2つの価値(+3.8s) → Point02(+4.6s) → **ドット停止(+6.9s)** → 役割名(+7.0s)

- ドットの時計は `S = A - miniAt`。`brakeStart = (autoEnd - miniAt) + moveDur + spinHold`
- `spinHold`（たっぷり回す時間・既定2.6秒）が新パラメータ。**旧 `spinDur` は廃止**
- セクションの尺 `visTotal` は時間割から**自動計算**（旧 `VIS_TOTAL` 定数は廃止）


## 📋 2026-08-15 の変更まとめ

### スクロールリプレイ復活
`replayCheck()` がセクションの矩形を毎フレーム見て、**完全に画面の外へ出たら時計を巻き戻す**。
戻ってくるともう一度アニメが流れる。調整パネル「🌊 全体 > リプレイ」で ON/OFF。
- 判定は `r.bottom <= 0 || r.top >= innerHeight`。⚠️ `>` だと画面高ちょうどの時に外れる（踏んだ）
- `progOverride[k]` が入っている時（検証中）は巻き戻さない

### ⚠️ 寄せ処理(snap)のハマりどころ（2回踏んだ）
所定の位置へ寄せる `lenis.scrollTo` を **毎フレーム発行してはいけない**。
- ❌ `st.snapping = elapsed` → elapsed が 0 のとき falsy で毎フレーム再発行
- ❌ `onComplete` で `snapT = null` → 次フレームでまた発行、永久に再生が始まらない
- ✅ `snapTried`(boolean) で **1回だけ発行**し、`snapT` からの経過が 1.2秒を超えたら諦めて再生開始

### 実績（カンプ 14801:31557）

| | カンプ | 実装 |
|---|---|---|
| 画像 | 左 -89.86 / 上 148.06 / 749.13×430 | ✅ 左-89.9 上148.1 幅749.1 |
| 右のまとまり | 左 748.7 / 上 246.06 / 幅 593 / gap 44 / 中央 | ✅ 完全一致 |
| 見出し | 「Anyflowが事業を推進させます。」Noto Sans JP **Light(300)** 40px | ✅ |
| 200+ / 500+ | SF Pro Text **Ultralight** 100px / 行間1 / 中央 | ✅ font-weight **100** |
| 導入企業 / 連携実績 | Noto Sans JP Light 16px / 行間24 / 右寄せ | ✅ |
| 下段 | 左 125.11 / 上 677.94 / gap 162 | ✅ |
| 本文 | Light 14px / 行間1.9 / #101828 / 幅653.086 | ✅ |
| 99.9% / 1week | SF Pro Text **Thin** 50px、稼働率側に右境界線 #d1d5dc | ✅ font-weight **200** |

**テキストは分けずに一括でブラー出現**し、3つの数字が同時にランダム→確定する（1week は演出なし）。

### 🔤 SF Pro のウェイト対応（実測で確定）
`canvas.measureText` で「200+」の幅を測ったところ、SF Pro Text は
**100 → 290.57 / 200 → 292.50 / 300 → 292.50 / 400 → 299.18**。
つまり **100 だけ別の字面(Ultralight)** で、200 と 300 は同じ字面になる。
Apple の並びは Ultralight < Thin < Light なので、対応は

| カンプの表記 | CSS |
|---|---|
| **Ultralight** | `font-weight: 100` |
| **Thin** | `font-weight: 200` |

⚠️ 2026-08-15 に一度 Thin を 100 にしてしまった（generic CSS の「100=Thin」に引きずられた）。**間違い**。

### 軌道グラフィック（KVとOur Visionで共通）
- **グラデの白い stop を削除**。ピンク #FF5D97 → 水色 #00ABEB がダイレクトにつながる
- **回り方の緩急** `ORBIT_EASES`（等速 / 弱 / 中 / 強）。
  φ = θ + a·sin(θ−90°) で写すと角速度が 1 + a·cos(θ−90°) になり、**手前で速く・奥でゆっくり**
- **軌道を流れるライト** `ORBIT_LIGHTS`（なし / ひと筋 / 連なり / 尾を引く）。
  破線を1本の光として扱い、`stroke-dashoffset` を毎フレームずらす。周長はラマヌジャンの近似式
- ⚠️ 光の楕円を足したので `querySelectorAll('.pf-wrap svg ellipse')` では本数が合わない。**id で取ること**（実際に壊れた）
- **ライトの太さ** は `params.orbitLightW`（既定 2.5px）。パネルの「ライトの太さ」で 1〜6px
- **グラデの継ぎ目がライトと一緒に動く**（2026-08-15）。
  グラデの軸を「中心→光の点」と**直角**に取ると、その点がちょうど 50%＝継ぎ目になる。
  ⚠️ 楕円は歪んでいるので「1周の何%(弧長)」と「媒介変数の角度」は比例しない。
  比例で済ませると光と継ぎ目が最大20°近くズレるため、**弧長の対応表**(`arcTable`/`angleAtArc`)で引き当てている。
  実測: ブラウザ自身の `getPointAtLength` で光の中心を出し、グラデ軸上の位置を測ると **0.500**（ぴったり）
- `updateOrbitSvg()` はグラデの向きを触らない。**`updateOrbitLights()` が毎フレーム決める**

### 惑星
パネルに **回す向き / 回転軸の傾き / ランダム** を追加。ランダムは周期の違う3つの波を重ねて、
繰り返しに聞こえない不規則さを作る（毎フレーム乱数を引くとガタガタになる）。

### Our Vision
- キーメッセージは **行まるごと**フェードイン（`charLag: 0`）。1文字ずつをやめてカクつきも解消
- 上下に分かれたあと**静止せず、慣性で流れ続けながら消える**（`driftAmt` / `vanishDur`）。
  実測: 0 → -28 → -162 → -193 → -208px と減速しながら流れ、その間に不透明度が 1 → 0
- 「2つの価値」→ Point 01 → Point 02 のディレイを 0.70 / 1.60 / 2.30 秒に拡大
- 中央そろえ実測: Our Vison / 1行目 / 2行目 とも **720px（画面中央ちょうど）**

### 尺の短縮（再生後の空スクロール対策）
再生中はスクロールを止めているので、`lenVh` はまるごと「再生後の空スクロール」になる。
- vision 240 → **150** / results 220 → **150** / cases 420 → **200**（playSec 8 → 5.4）

### 導入事例
カードの送りを `seg(p, 0.06, 0.94, easeIO)` → `seg(p, 0.08, 0.72, easeSlick)` に。
尺いっぱいの easeIO は出だしも終わりも遅く「のっそり」して見えた。

### 開発者体験の追加変更
- 見出しを 143.8 → **183.8px** に下げて、コピーとモックの間を **75.2 → 35.7px**（40px詰め）
- モックと右リストのまとまりを**中央揃え**に（左右余白とも 105.0px）。カンプは25.3px左寄りだった
- スキップは**上に1個**だけ。押すと **ブラーが山なりに膨らんだ頂点で中身が入れ替わる**（実測 5.6→26.3→0）
- **見出しの文字が出るまでアニメは始まらず、モックの中身も空**（`.is-blank` で AI Assistant のヘッダー・
  アイコン・入力欄まで隠す）。章②に入る時も一度空に戻す


## 🐛 2026-08-15「2つの価値の後にビジョンへ戻るループ」の原因

スクロールリプレイを入れたことで **通り過ぎたセクションの時計が巻き戻る** ようになった。
ところが再生開始の条件が **「進捗 > 0.002」だけ** だったため、
通り過ぎて進捗が 1 に近いままでも「開始できる」と判定され、
`playT` の位置合わせ `lenis.scrollTo(sec.offsetTop)` が **ユーザーをセクションの頭へ引き戻していた**。

対策は3つ：
1. `canPlay(p)` = `p > 0.0005 && p < 0.35` — **固定区間の頭にいる時だけ**開始できる
2. 位置合わせは **1画面以内にいる時だけ**。それより遠ければ何もしない
3. ⚠️ 寄せた直後は進捗がぴったり 0 に戻るので `canStart` が false になる。
   **一度寄せ始めたら `canStart` は見ない**（`canStart || st.snapTried`）。
   これを入れないと「寄せたのに永久に再生が始まらない」になる（実際に踏んだ）

## 📖 ビジョンのキーメッセージ（2026-08-15 更新）

### 出方は2種類（調整パネル「キーメッセージの出方」）
| | 中身 |
|---|---|
| **下から立ち上がる**（既定） | 行の箱でマスクし、文字が下から出てくる。1文字ずつ遅れる（`charLag` 0.5） |
| **その場でブラー出現**（**既定**） | 動かさず、**最終位置のまま**ブラーと不透明度だけで出る。「Our Vison」ラベルもスライドしない |

2026-08-15 の指定で **既定は「その場でブラー出現」＋ `charLag: 0`（行ごと）**。
つまり日本語の2行は**位置移動なし・行まるごとブラー出現**。

### 待ちを詰めた
| | 前 | 今 |
|---|---|---|
| 2行そろってから分かれるまで | 0.25s | **0**（止まらずそのまま開く） |
| 消え出すまで | 分割の50% | **分割の15%** |
| 消えきるまで | 1.3s | **0.85s** |
| たっぷり回す時間 | 2.6s | **1.9s** |
| 2つの価値 / Point01 / Point02 | 0.70 / 1.60 / 2.30s | **0.45 / 0.95 / 1.35s** |

### 軌道は「濃くなりながら同時に左下へ」
- `moveLead: 0` — 軌道が**出はじめた時点でもう動き出す**
- ⚠️ 移動のイージングは `easeIO` → **`easeSlick`**。easeIO は出だしが遅いので、
  濃くなり終わってからやっと動き出すように見えていた
- 実測 [不透明/横半径]: 0.25/127 → 0.66/162 → 0.86/211 → 0.96/263 → 1.00/408（同時に伸びている）


## 🎛 調整パネルの構造（2026-08-15 全面整理）

### 反映はすべてリアルタイム
スライダーは `params` を直接書き換え、描画ループが毎フレーム `params` を読むので**リロード不要**。
惑星のデザイン切替も、シェーダーは起動時に全デザインぶんコンパイル済み（`progs[params.design]`）なので即座に変わる。

### 階層
```
🌊 全体      リプレイ / 滑らかさ
🖼 キービジュアル
   文字の登場   ヘッダー〜ブラー出現の速さ
   🪐 惑星      デザイン(A1〜C3)
     └ 柄の動き   ← デザインに応じて「ハーフトーンの粒」or「色の帯の流れ方」に入れ替わる
     └ ライト
     └ 自転      1回転の秒数 / 回す向き / 回転軸の傾き / ランダム / 多面ゆらぎ / 質感の粒
     └ 位置と大きさ
   🛰 軌道      全体のX/Y
     └ ゆらぎ
     └ ドットの回り方  等速・緩急 / 1周の秒数 / 倍率 / なめらか加速 / 回転方向 / 速さのゆらぎ
     └ 外側の楕円 / 内側の楕円
📖 ビジョン / 📈 実績 / 🖥 開発者体験 / 🗂 導入事例
```
- `sub(target, html, true)` で**一段内側の小見出し**（`.grp.sub2`）になる
- `slider(..., fmt, hint)` の第8引数が**補足文**（`.row-hint`）。全スライダーに入れてある（74箇所）
- **ツマミの既定位置は自動で中央**（2026-08-15）。`defaultOf(get)` が
  「一瞬だけ `params` を `DEFAULTS` に差し替えて get を呼ぶ」ことで既定値を取り、
  `max = 2 × 既定 − min` に取り直している。**呼び出し側は何も書かなくてよい**。
  実測 74本中 67本が中央。残り6本は**既定が0**のもの（ランダム/ゆれ幅/ゆれ位相/軌道が動き出す/1文字ずつの遅れ）で、
  マイナスに意味がないため中央にできない
- ⚠️ 「尺」は既定が min と同じで下げられなかったので、min をピンが成立する **110vh** まで下げてある
- `note(text)` はグループ直下の注釈。ピルの選択中の説明を出すのに使っている

### 惑星デザインのキー（2026-08-15 振り直し）
| 旧 | 新 | 中身 |
|---|---|---|
| B / C / D | **A1 / A2 / A3** | テクスチャ系（ビビッドブルー / ピンク×ブルー / ライトグレイン） |
| E | **B** | ハーフトーン（フル生成） |
| F / G / H | **C1 / C2 / C3** | 波が流れる（`waveFragment`） |

`WAVE_DESIGNS = ['C1','C2','C3']`、ディザ演出の判定は `params.design === 'B'`。
既定は **B ハーフトーン / ゆらぎ=③クロス**。

### 廃止したもの
- **軌道を流れるライト**（`ORBIT_LIGHTS` / `arcTable` / `setGradSeam` / `.orbit-light`）は全削除。
  グラデの向きは `updateOrbitSvg()` が縦軸に戻す。
  **軌道のグラデの白は 2026-08-15 にカンプ通り復活**（カンプ `14820:23875` の SVG 実測で確認）:
  | | ストップ |
  |---|---|
  | KV 内側 | `#00ABEB` → `white @0.490385` → `#FF5D97` |
  | KV 外側 | `#FF5D97` → `white @0.403846` → `#00ABEB` |
  | ビジョン pfG0 | `#FF5D97` → `#FFCFE0 @0.370192` → `white @0.490385` → `#A2E6FF @0.600962` → `#00ABEB` |
  | ビジョン pfG1 | `#00ABEB` → `#A2E6FF @0.399038` → `white @0.509615` → `#FFCFE0 @0.629808` → `#FF5D97` |
- パネルの「ロゴのティッカー」グループ（`params.marquee` 自体は残っている）
- 軌道の緩急は4段階 → **等速 / 緩急 の2つ**だけ

## 📈 実績のレスポンシブとパララックス（2026-08-15）

- テキストは **1440px の枠 `.res-wrap` ごと画面中央** に置く。中の座標はカンプのままなので**比率が変わらない**
- 画像だけ**画面の左端まで伸びる**。右端はカンプ通り 659.27px で止まる
  ```css
  left:  calc((1440px - var(--swp)) / 2 - 89.86px);
  width: calc(749.13px + (var(--swp) - 1440px) / 2);
  ```
  ⚠️ `img` は置換要素なので **left + right を指定しても伸びない**（width が固有値のまま）。**幅を計算で出すこと**
- **パララックス**は `object-position` を動かす。枠は動かさないのでレイアウトはまったく崩れない。
  `params.sections.results.parallax`（既定16%）。実測 `50% 48.46%` → `50% 52.57%`
- 実測 1800×760: 画像 左-74/幅924（伸びている）、下段の左余白409 / 右余白389（中央）

## 🐛 開発者体験が真っ白になる（2026-08-15・2回目）

白へ戻す `fin` を**生のスクロール `pRaw` に直結**していた。
章と章の間はロックが外れるので **pRaw だけ先へ進んでしまい、まだ再生中なのに背景が真っ白**になっていた。
→ 全部終わった地点の pRaw を `st.endP` に覚えて、**そこからの差分**で白くする（`DEV_FIN = 0.30`）。

## 8. 既知の注意点

- Claude の Browser プレビューでは rAF が止まる（実ブラウザでは動く）。過去に何度も混乱したので注意
- Figma の惑星画像を差し替える時は 4倍スケールで書き出し、`DESIGNS[].src` を更新
- E のディザは「色の場を動かす」実装。しきい値やパターンだけ動かすとユーザーNG（表層的に見える）
- **F/G/H = 波が流れる惑星**（カンプ `14776:26144`／2026-08-14 追加）。
  E と同じハーフトーン(ベイヤーディザ)のまま「色の場」だけ差し替えたもの。
  `waveFragment(bands, warp)` でシェーダーを生成し、F=0.55/0.06、G=1.05/0.30、H=1.75/0.42。
  左下(青)→右上(ピンク)の軸に色の帯を並べ、`uTime` で流す。
  流れ方は `params.dither` を共用（`WAVE_FLOWS`: 止まる/ゆっくり/流れる/波打つ）。
  **既定は E のまま**（採用が決まったら `DEFAULTS.design` を変える）
- ライト②の影はグレーでなく青みに沈める（`shadowCol = col * vec3(0.55, 0.60, 0.85)`）
- コミットメッセージ `d4d92ee` の直後の履歴に「Auto-save」コミットが混ざることがある（デスクトップアプリの自動保存）。実害なし
